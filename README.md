# 11 laba

## Условие задачи
С одномерным массивом, состоящим из n вводимых с клавиатуры положительных и отрицательных целых чисел выполнить следующие вычисления:
Сумму индексов элементов массива равных заданному числу А.

## 1. Реализация программы

```
#include <stdio.h>
#include <locale.h>

#define n 10

int main() {
    setlocale(LC_CTYPE, "RUS");

    int A, sum = 0;
    int arr[n];

    printf("Введите %d целых чисел (положительных и отрицательных):\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Введите число A: ");
    scanf("%d", &A);

    for (int i = 0; i < n; i++) {
        if (arr[i] == A) {
            sum += i;
        }
    }

    printf("Сумма индексов элементов, равных %d: %d\n", A, sum);
    return 0;
}
```

## 3. Результаты работы программы
```
Введите 10 целых чисел (положительных и отрицательных):
1
2
3
1
2
3
1
2
3
1
Введите число A: 1
Сумма индексов элементов, равных 1: 18
```

## 4. Информация о разработчике

Юрьев Денис [БИЦ-252]
